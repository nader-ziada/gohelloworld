apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: gohelloworld
  namespace: default
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: gcr.io/<project>/gohelloworld
            env:
            - name: TARGET
              value: "Go Sample v1"